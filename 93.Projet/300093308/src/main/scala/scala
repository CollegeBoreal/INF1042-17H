 object Main {
   def main(args: Array[String]): Unit = {

 resultat = tri_bulle(tableau T){

 }
         entier longueur, i
          booleen inversion

          longueur<-taille(T)

          faire
              inversion=faux
              pour i=0 à (longueur-1)
                  si T(i)>T(i+1)
                      echanger(T,i,i+1)
                      inversion<-vrai{
}

  tri_bulle_optimise(tableau T)
      debut
          entier longueur, i
          booleen inversion

          longueur<-taille(T)

          faire
              inversion<-faux
              pour i=0 à (longueur-1)
                  si T(i)>T(i+1)
                      echanger(T,i,i+1)
                      inversion<-vrai
                  fin si
                  longueur<-longueur-1

   }
 }